// Word groups
const wordGroups = {
    group1: ['apple', 'banana', 'cherry'],
    group2: ['dog', 'elephant', 'frog'],
    // Add more groups as needed
};

// Digraphs
const digraphs = {
    'ch': 'ch',
    'sh': 'sh',
    'th': 'th',
    // Add more as needed
};

// Compliments
const compliments = [
    'Great job!',
    'Well done!',
    'Keep it up!',
];

// Badge names
const badgeNames = {
    bronze: 'Bronze Badge',
    silver: 'Silver Badge',
    gold: 'Gold Badge',
};

// Configuration constants
const configConstants = {
    maxAttempts: 5,
    minWordLength: 3,
    timeoutDuration: 3000,
};

// Exporting all constants
export { wordGroups, digraphs, compliments, badgeNames, configConstants };